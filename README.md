# Визуализация данных сенсоров и пространственных данных
 
Веб-приложение на Flask собирает данные с двух серверов [OGC SensorThings API](https://www.ogc.org/standards/sensorthings) и визуализирует их на интерактивной карте с дашбордом. Источники данных:
- БД PostgreSQL с даннными с FrostServers (docker image: `uroplatus666/frosts-db:latest`)
- БД PostgreSQL с векторными и растровыми данными (docker image: `uroplatus666/gis-db:latest`)
- Загрузка данных из FROST серверов в БД в режиме real time
- Парсинг данных с (sensor-community)[https://archive.sensor.community/], обработка и загрузка на FROST в режиме real time

## Возможности

- Интерактивная карта на базе Folium со слоями для каждого сенсора SensorThings;
- Попапы с переключением MultiDatastreams / Datastreams и ссылками на подробный дашборд;
- Подготовка временных рядов по всем измерениям и расчёт розы ветров по данным сервера РУДН;
- API `/api/data/<sensor_key>` для фронтенда дашборда и сторонних клиентов;
- Готовые HTML-дашборды с графиками Plotly и карточками последних измерений;
- Растровые и векторные слои
- Хорошая скорость
- Свежие данные

## 🧩Структура репозитория

```
.
├──frontend/
│   ├── .env                  # Переменные окружения
│   ├── .dockerignore         # Исключения для Docker
│   ├── .gitignore            # Исключения для Git
│   ├── docker-compose.yml    # Docker Compose со сборкой и прокидыванием окружения
│   ├── pyproject.toml        # Метаданные и зависимости
│   ├── uv.lock               # Блокировка версий зависимостей
│   ├── Dockerfile            # Docker-образ
│   ├── README.md             # Комментарии
│   ├── app.py                # Корневой файл
│   ├── config.py             # Конфигурация
│   ├── services/             # Логика
│   │   ├── __init__.py
│   │   ├── gis.py            # Работа с БД геоданных и обработка данных
│   │   └── sensors.py        # Работа с БД сенсоров и обработка данных
│   ├── static/               # Статика
│   │   ├── css/
│   │   │   ├── dashboard.css # Стили для страницы дашборда
│   │   │   └── map.css       # Стили для карты
│   │   └── js/
│   │       └── dashboard.js  # Скрипты графиков
│   └── templates/            # HTML-шаблоны
│       ├── dashboard.html    # Шаблон страницы дашборда
│       └── map_partials/     # Куски HTML/JS для вставки в Folium
│           ├── css_inject.html
│           └── js_inject.html
│
├──bd-loaders/
│   ├── loader-rudn/          # Загрузка данных в БД c FROST HSE
│   │   ├── .env              # Переменные окружения
│   │   ├── Dockerfile        # Docker-образ
│   │   ├── config.py         # Интервалы, локации, сенсоры
│   │   ├── ingest-frost.py   # Загрузка данных
│   │   ├── pyproject.toml    # Метаданные и зависимости
│   │   └── uv.lock           # Блокировка версий зависимостей
│   └── loader-hse/           # Загрузка данных в БД c FROST РУДН
│       ├── .env              # Переменные окружения
│       ├── Dockerfile        # Docker-образ
│       ├── config.py         # Интервалы, локации, сенсоры
│       ├── ingest-frost.py   # Загрузка данных
│       ├── pyproject.toml    # Метаданные и зависимости
│       └── uv.lock           # Блокировка версий зависимостей
│
└──sensor-community/
    ├── app/
    │   ├── __init__.py
    │   ├── config.json         # Сенсоры и диапазоны загрузки + другие данные
    │   ├── main.py             # основной файл
    │   ├── scraper.py          # Парсер (Extract)
    │   ├── processor.py        # Обработка и геокодирование (Transform)
    │   ├── uploader.py         # Загрузка (Load)
    │   └── requirements.txt
    ├── data_archive/           # Папка на хосте для сохранения CSV и логов
    │   ├── SDS011/             # Папка для данных сенсоров SDS011, создается автоматически
    │   ├── BME280/             # Папка для данных сенсоров BME280, создается автоматически
    │   ├── all_stats.xlsx      # Общий файл с метаданными
    │   ├── description.xlsx    # Исходный файл с описаниями датчиков
    │   └── state.json          # Состояние по выгрузкам - последняя дата загрузки для каждого датчика
    ├── .env                    # Секреты
    ├── Dockerfile              # Инструкция сборки образа
    └── README.md               # Комментарии
```

## Переменные окружения

- В файле `frontend/.env.example` приведена структура файла .env, куда можно ввести свои секреты

| Переменная              | Назначение                                  | Значение по умолчанию             |
|-------------------------|---------------------------------------------|-----------------------------------|
| `DB_HOST`               | совпадает с именем сервиса в docker-compose |db-frost                           |
| `DB_PORT`               | Порт для БД с данными с FrostServers        |5432                               |
| `DB_EXTERNAL_PORT`      | Порт для БД с данными с FrostServers        |5434                               |
| `DB_NAME`               | Имя БД с данными с FrostServers             |<DB_NAME>                          |
| `DB_USER`               | Пользователь БД с FrostServers              |<DB_USER>                          |
| `DB_PASS`               | Пароль от БД с FrostServers                 |<DB_PASS>                          |
| `PGHOST`                | совпадает с именем сервиса в docker-compose |db-spatial                         |
| `PGPORT`                | Порт для БД с данными с геоданными          |5432                               |
| `PG_EXTERNAL_PORT`      | Порт для БД с данными с геоданными          |5433                               |
| `PGDATABASE`            | Имя БД с геоданными                         |<PGDATABASE>                       |
| `PGUSER`                | Пользователь БД с геоданными                |<PGUSER>                           |
| `PGPASS`                | Пароль от БД с геоданными                   |<PGPASS>                           |
| `PORT`                  | Порт на хосте для проброса Docker           |9090                               |

- В файле `bd-loaders/loader/.env.example` приведена структура файла .env, куда можно ввести свои секреты

| Переменная              | Назначение                                  | Значение по умолчанию             |
|-------------------------|---------------------------------------------|-----------------------------------|
| `MAPBOX_TOKEN`          | Токен от Mapbox для геокодирования          |<YOUR_TOKEN>                       |

- В файле `frontend/.env.example` приведена структура файла .env, куда можно ввести свои секреты

| Переменная              | Назначение                                 | Значение по умолчанию                      |
|-------------------------|--------------------------------------------|--------------------------------------------|
| `FROST_URL`             | Адрес FROST сервера                        |http://90.156.134.128:8080/FROST-Server/v1.1|
| `PG_DSN`                | Строка подключения к базе данных PostgreSQl|postgresql://frost:frost@db-frost:5432/frost|
| `LOG_LEVEL`             | Уровень логирования                        |INFO                                        |

- В файле `bd-loaders/loader-rudn//.env.example` приведена структура файла .env, куда можно ввести свои секреты

| Переменная              | Назначение                                 | Значение по умолчанию                      |
|-------------------------|--------------------------------------------|--------------------------------------------|
| `FROST_URL`             | Адрес FROST сервера                        |http://94.154.11.74/frost/v1.1              |
| `PG_DSN`                | Строка подключения к базе данных PostgreSQl|postgresql://frost:frost@db-frost:5432/frost|
| `LOG_LEVEL`             | Уровень логирования                        |INFO                                        |
| `PAGE_TIMEOUT`          | Максимальное время ответа от FROST серевера|120                                         |
| `BATCH_SIZE`            | Количество данных, скаченных за раз с FROST|2000                                        |


### Интервалы загрузки `load_interval`, а также даты, локации/датастримы можно задать в конфигах каждого загрузчика.
Сейчас стоит:
- `sensor-community`: 24 часа (86400 сек)
- `loader/loader`: 5 мин (300 сек)
- `loader/loader-rudn`: 5 мин (300 сек)

## Запуск в Docker 

```bash
# Запустите эту команду из frontend/
docker compose up -d
```
Проверка, что самая тяжелая БД догрузилась (ждем `database system is ready to accept connections`)

```bash
docker logs -f db-spatial
```

Сервер здесь: `http://127.0.0.1:<port>/`.

Остановка, удаление контейнеров и volumes

```bash
docker compose down -v
```
